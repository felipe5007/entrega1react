import { FaShoppingCart } from "react-icons/fa";

const Cartwidget = ({ cartCounter }) => {
  return (
    <>
      <button className="">
        {cartCounter}
      </button>
    </>
  );
};
export default Cartwidget;